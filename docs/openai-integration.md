# OpenAI API integration

The Android application must never contain the project `OPENAI_API_KEY`. Cloud AI is an explicit opt-in capability behind a server-side VVF gateway. Local/on-device semantic search remains the default.
