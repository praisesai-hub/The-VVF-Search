name: CodeQL

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "30 2 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: ["java-kotlin"]
        build-mode: ["manual"]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          validate-wrappers: true

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      # CodeQL must observe a fresh Kotlin compilation between init and analyze.
      # Kotlin's default compiler daemon and cached outputs can hide compilation
      # from the CodeQL tracer, causing an empty Java/Kotlin database.
      - name: Build for CodeQL analysis
        run: |
          ./gradlew --no-daemon --no-build-cache :app:clean :app:assembleDebug \
            -Pkotlin.incremental=false \
            -Pkotlin.compiler.execution.strategy=in-process \
            -Dkotlin.caching.enabled=false

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
